package org.leanpoker.player.model;

import java.util.List;
//import java.util.stream.Collectors;

public class CombinationFinder {
    public static int getFinestCombination(List<HoleCard> cards) {
        int weight = 0;

  /*      if (cards.stream().map(HoleCard::getRank).collect(Collectors.toList()))) {

        }
*/
        return weight;
    }
}
